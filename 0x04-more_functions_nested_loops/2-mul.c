#include "main.h"
#include <stdio.h>
/**
 * int mul(int a, int b) will multiply two numbers
 * @a: is one of the nbrs to multiply
 * @b: is the nbr to multiply it with
 * Return always = 0
 * */
int mul(int a, int b)
{
    return (a * b);
}
