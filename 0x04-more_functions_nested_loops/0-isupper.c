#include "main.h"
/**
 * int _issupper(int c) analyses if a string is upper case and
 * return 1 if c is uppercase
 * return 0 otherwise
 * @c: The number to be checked
 */

int _isupper(int c)
{
	
	if (x >= 65 && x <= 90)
	{
		return (1);
	}

	return (0);
}
